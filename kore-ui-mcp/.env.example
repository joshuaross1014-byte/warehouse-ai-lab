# Copy to .env and fill in your own values. Never commit the real .env.
DB_SERVER=localhost      # SQL Server host\instance backing HJ One
WEBUI_DB=HJOneCore       # HJ One / Körber One web-UI definition database
DB_USER=your_sql_login
DB_PASS=your_sql_password
